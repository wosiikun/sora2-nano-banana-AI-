# AI动画剧情工程管理器 - 项目结构说明

## 📋 项目概述

这是一个基于Streamlit的AI动画剧情工程管理系统，通过AI自动生成视频剧本、分镜脚本和图片素材，支持图生图功能，实现从主题到完整素材的一键生成。

**核心功能：**
- ✅ 5步自动生成剧本和分镜
- ✅ 一键生成所有素材（人物、场景、分镜图）
- ✅ 图生图支持（多角度图+场景图→分镜图）
- ✅ 多模型选择（sora-image、gpt-image-1.5、nano-banana等）
- ✅ 工程管理和素材管理

---

## 📁 项目目录结构

```
mjj/
├── app_project.py              # 主应用文件（Streamlit界面）
├── agent.py                    # AI Agent处理逻辑
├── api_client.py               # DeepSeek API客户端
├── image_generator.py          # GrsAI图片生成模块
├── image_uploader.py           # 图片上传中转模块
├── asset_generator.py          # 素材生成器（调用image_generator）
├── project_manager.py          # 工程管理器
├── prompts.py                  # 提示词模板
├── test.py                     # 图生图测试工具
│
├── projects/                   # 工程数据目录
│   └── *.json                  # 工程JSON文件（按时间戳命名）
│
├── images/                     # 生成的图片素材目录
│   └── {project_id}/
│       ├── characters/         # 人物图片
│       │   ├── {char_id}_base.png      # 人物基础图
│       │   └── {char_id}_views.png     # 人物多角度图
│       ├── scenes/             # 场景图片
│       │   └── {scene_name}.png
│       └── storyboard/         # 分镜图片
│           └── segment_{id}.png
│
├── test_results/               # 测试结果目录
├── uploaded_refs/              # 上传的参考图片
└── __pycache__/               # Python缓存文件
```

---

## 📄 核心文件功能说明

### 1. **app_project.py** (主应用文件，1330行)
**功能：** Streamlit Web应用主界面，整合所有功能模块

**主要组件：**
- **配置部分：**
  - DeepSeek API配置（用于文本生成）
  - GrsAI API配置（用于图片生成）
  - 图片上传服务器配置

- **核心类：**
  - `ImageGenerator`: 图片生成器，支持图生图
    - `generate()`: 生成图片（支持参考图片URL列表）
    - `poll_result()`: 轮询任务结果
    - `download_image()`: 下载生成的图片
  
  - `DeepSeekClient`: DeepSeek API客户端
    - `call()`: 调用DeepSeek API生成文本
  
  - `StoryAgent`: 故事生成Agent
    - `step1_generate_script_prompt()`: 生成文案提示词
    - `step2_generate_script()`: 生成文案
    - `step3_generate_base_storyboard()`: 生成基础分镜JSON
    - `step4_extract_characters_scenes()`: 提取人物场景提示词
    - `step5_generate_final_storyboard()`: 生成完整分镜JSON
  
  - `ProjectManager`: 工程管理器
    - `create()`: 创建新工程
    - `load()`: 加载工程
    - `save()`: 保存工程
    - `update_*()`: 更新各类数据（人物、场景、分镜等）

- **辅助函数：**
  - `upload_image_to_server()`: 上传图片到中转服务器
  - `parse_character_scene_prompts()`: 解析人物场景提示词
  - `one_click_generate_assets()`: 一键生成所有素材
  - `extract_characters_from_segments()`: 从分镜中提取人物
  - `extract_scenes_from_segments()`: 从分镜中提取场景

- **UI界面：**
  - **侧边栏：** 工程管理（新建、打开、保存）
  - **Tab1 - 剧本生成：** 5步自动生成流程
  - **Tab2 - 一键生成：** 批量生成素材（基础模式/完整模式）
  - **Tab3 - 素材管理：** 人物、场景、分镜的查看和编辑
  - **Tab4 - 设置：** API配置和工程导出

**关键特性：**
- 支持图生图（多参考图合成）
- 自动添加4宫格前缀（分镜图）
- 进度条显示生成进度
- 对话记录保存和查看

---

### 2. **agent.py** (Agent处理逻辑)
**功能：** 封装AI Agent的5步处理流程

**主要方法：**
- `step1_generate_script_prompt(theme)`: 根据主题生成文案提示词
- `step2_generate_script(script_prompt)`: 根据提示词生成文案
- `step3_generate_base_storyboard(script, theme)`: 生成基础分镜JSON
- `step4_extract_characters_scenes(base_json)`: 提取人物和场景提示词
- `step5_generate_final_storyboard(base_json, character_scene)`: 生成完整分镜JSON

**特点：**
- 自动清理JSON格式（去除markdown标记）
- 错误处理（JSON解析失败时返回原始文本）

---

### 3. **api_client.py** (DeepSeek API客户端)
**功能：** 封装DeepSeek API调用

**主要方法：**
- `call(prompt, system_msg)`: 调用DeepSeek API生成文本

**配置：**
- API Key: `sk-a2e28559994a4129bf6b2282ba034e6e`
- Base URL: `https://api.deepseek.com`
- Model: `deepseek-chat`

---

### 4. **image_generator.py** (图片生成模块)
**功能：** 调用GrsAI API生成图片

**主要函数：**
- `generate_image(prompt, model, aspect_ratio, image_size, save_path)`: 生成图片
- `poll_result(task_id)`: 轮询任务结果（带进度显示）
- `download_image(image_url, save_path)`: 下载图片到本地

**支持的模型：**
- GPT模型：`sora-image`, `gpt-image-1.5`
- Banana模型：`nano-banana-fast`, `nano-banana-pro`

**特点：**
- 支持不同图片比例（1:1, 16:9, 9:16等）
- 支持不同图片尺寸（1K, 2K）
- Streamlit进度条显示

---

### 5. **image_uploader.py** (图片上传模块)
**功能：** 将本地图片上传到中转服务器，获取URL用于图生图

**主要函数：**
- `upload_image_to_server(image_path, server_url)`: 上传单张图片
- `upload_multiple_images(image_paths, server_url)`: 批量上传图片

**特点：**
- 自动检测文件类型（PNG、JPG、GIF、WebP）
- 错误处理和重试机制

---

### 6. **asset_generator.py** (素材生成器)
**功能：** 封装素材生成逻辑，调用image_generator生成各类素材

**主要方法：**
- `generate_character_base()`: 生成人物基础图
- `generate_character_views()`: 生成人物多角度图（图生图）
- `generate_scene()`: 生成场景图
- `generate_storyboard()`: 生成分镜图（多图合成）

**特点：**
- 自动将生成的图片移动到工程目录
- 统一的文件命名规范

---

### 7. **project_manager.py** (工程管理器)
**功能：** 管理工程数据的创建、加载、保存和更新

**主要方法：**
- `create(theme)`: 创建新工程
- `load(project_id)`: 加载工程
- `save(data)`: 保存工程
- `list_projects()`: 列出所有工程
- `update_script()`: 更新文案
- `update_segments()`: 更新分镜
- `update_character()`: 更新人物数据
- `update_scene()`: 更新场景数据
- `update_segment_image()`: 更新分镜图片路径

**数据结构：**
```json
{
  "id": "20260124_144221",
  "theme": "主题描述",
  "created_at": "2026-01-24T14:42:21.003547",
  "script": "视频文案",
  "base_json": [],
  "characters": {
    "js1": {
      "prompt": "人物提示词",
      "base_img": "图片路径",
      "view_img": "图片路径"
    }
  },
  "scenes": {
    "场景名": {
      "prompt": "场景提示词",
      "img": "图片路径"
    }
  },
  "segments": [
    {
      "segment_id": 1,
      "time_range": "0s-4s",
      "edesc": "图片生成提示词",
      "videodesc": "视频生成提示词",
      "cap": "分镜描述",
      "js": "js1,js2",
      "local": "场景名",
      "img": "图片路径"
    }
  ],
  "conversations": [],
  "one_click_settings": {
    "model": "sora-image",
    "character_size": "1:1",
    "scene_size": "16:9",
    "storyboard_size": "16:9"
  }
}
```

---

### 8. **prompts.py** (提示词模板)
**功能：** 定义5步流程的提示词模板

**主要模板：**
- `step1_template()`: 生成文案提示词的模板
- `step3_template(script, theme)`: 生成基础分镜JSON的模板
- `step4_template(base_json)`: 提取人物场景提示词的模板
- `step5_template(base_json, character_scene)`: 生成完整分镜JSON的模板

**特点：**
- 详细的提示词设计，确保AI输出格式正确
- 支持中英文混合

---

### 9. **test.py** (测试工具)
**功能：** 图生图功能测试工具

**主要功能：**
- 纯文生图测试
- 图生图测试（支持多参考图）
- 代理服务器连接测试
- 参考图库管理

**使用场景：**
- 测试API连接
- 测试图生图效果
- 调试图片上传功能

---

## 🔄 工作流程

### 剧本生成流程（5步）

1. **Step 1: 生成文案提示词**
   - 输入：主题（如"废土主题，主角女，配角男，15s"）
   - 输出：完整的文案生成提示词

2. **Step 2: 生成文案**
   - 输入：Step1生成的提示词
   - 输出：视频文案（一段完整的剧情描述）

3. **Step 3: 生成基础分镜**
   - 输入：文案 + 主题
   - 输出：基础分镜JSON（包含segment_id、time_range、edesc、videodesc、cap、js、local）

4. **Step 4: 提取人物场景**
   - 输入：基础分镜JSON
   - 输出：人物提示词 + 场景提示词（自动解析并保存到工程数据）

5. **Step 5: 生成完整分镜**
   - 输入：基础分镜JSON + 人物场景提示词
   - 输出：完整分镜JSON（整合后的最终版本）

### 素材生成流程

**基础模式（simple）：**
1. 生成人物基础图（根据人物提示词）
2. 生成人物多角度图（图生图，以基础图为参考）
3. 生成场景图（根据场景提示词）

**完整模式（advanced）：**
1. 执行基础模式的所有步骤
2. 生成分镜图（图生图，以人物多角度图+场景图为参考）

---

## 🔧 技术栈

- **前端框架：** Streamlit
- **AI文本生成：** DeepSeek API (deepseek-chat)
- **AI图片生成：** GrsAI API (sora-image, gpt-image-1.5, nano-banana)
- **图片上传：** 自定义中转服务器
- **数据存储：** JSON文件
- **Python库：** 
  - streamlit
  - openai (DeepSeek客户端)
  - requests (HTTP请求)
  - pathlib (文件路径处理)
  - json (数据序列化)

---

## 📝 使用说明

### 启动应用
```bash
streamlit run app_project.py
```

### 基本使用流程

1. **创建工程**
   - 在侧边栏输入主题，点击"创建"

2. **生成剧本**
   - 切换到"📝 剧本生成"标签
   - 点击"🚀 生成剧本"按钮
   - 等待5步流程完成

3. **生成素材**
   - 切换到"🎯 一键生成"标签
   - 配置生成参数（模型、比例等）
   - 选择模式（基础素材/完整素材）
   - 点击生成按钮

4. **管理素材**
   - 切换到"📊 素材管理"标签
   - 查看、编辑、重新生成各类素材

5. **导出工程**
   - 切换到"⚙️ 设置"标签
   - 下载工程JSON文件

---

## 🎯 核心特性

### 1. 图生图支持
- 支持多参考图输入
- 人物多角度图生成（以基础图为参考）
- 分镜图生成（以人物多角度图+场景图为参考）
- 自动添加4宫格前缀（分镜图专用）

### 2. 智能跳过
- 一键生成时自动跳过已生成的素材
- 只生成缺失的素材，节省时间和成本

### 3. 进度显示
- 实时显示生成进度
- 状态提示（正在生成哪个素材）

### 4. 对话记录
- 保存所有AI对话历史
- 支持查看和调试

### 5. 多模型支持
- sora-image（GPT模型）
- gpt-image-1.5（GPT模型）
- nano-banana-fast（Banana模型，快速）
- nano-banana-pro（Banana模型，高质量）

---

## 📌 注意事项

1. **API配置：** 需要在代码顶部修改API Key和服务器地址
2. **图片上传服务器：** 需要确保中转服务器正常运行
3. **文件路径：** 使用相对路径，确保目录结构正确
4. **模型限制：** Banana模型不支持图生图，会自动切换到sora-image
5. **超时设置：** 默认超时300秒，可根据需要调整

---

## 🔍 项目特点总结

- ✅ **模块化设计：** 各功能模块独立，便于维护
- ✅ **完整流程：** 从主题到素材的完整自动化流程
- ✅ **图生图支持：** 先进的图生图功能，保证素材一致性
- ✅ **用户友好：** Streamlit界面，操作简单直观
- ✅ **数据持久化：** JSON文件存储，便于备份和迁移
- ✅ **可扩展性：** 易于添加新功能和模型支持

---

## 📚 相关文件说明

- **mjj/目录：** 可能是旧版本或备份目录
- **projects/目录：** 存储所有工程JSON文件
- **images/目录：** 存储所有生成的图片素材
- **test_results/目录：** 测试工具生成的图片
- **uploaded_refs/目录：** 用户上传的参考图片

---

*最后更新：2026-01-24*
